{
    "name": "infosec-compliance-chat",
    "version": "1.0.0",
    "description": "Infosec Compliance Chat",
    "scripts": {
      "build": "node build.js",
      "dev": "cp config.local.js config.js || node build.js && npx serve ."
    },
    "dependencies": {
      "dotenv": "^16.0.3",
      "fs-extra": "^11.1.1"
    },
    "devDependencies": {
      "serve": "^14.2.0"
    },
    "engines": {
      "node": ">=14.0.0"
    }
  }